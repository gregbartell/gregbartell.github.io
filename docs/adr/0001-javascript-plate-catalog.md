# Use a JavaScript Plate Catalog

The plate collection is a static site with no build step, but the plate facts
need one authoritative home. The site keeps plain HTML and CSS for the page
shell and renders plate cards, checklist data, status badges, image paths, and
missing-photo placeholders from a small vanilla JavaScript Plate Catalog so
catalog facts stay local without introducing a framework or build pipeline.
